# Configurações do Servidor
PORT=5000
NODE_ENV=development

# URL do Frontend (para CORS)
FRONTEND_URL=http://localhost:3000

# Configurações do Banco de Dados MySQL
DB_NAME=consorcio_manager
DB_USER=root
DB_PASSWORD=root
DB_HOST=localhost
DB_PORT=3306

# Configurações JWT
JWT_SECRET=sua_chave_secreta_super_forte_aqui_com_pelo_menos_32_caracteres
JWT_EXPIRES_IN=24h

# Exemplo de comandos para criar o banco:
# CREATE DATABASE consorcio_manager CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
# CREATE USER 'consorcio_user'@'localhost' IDENTIFIED BY 'senha_forte';
# GRANT ALL PRIVILEGES ON consorcio_manager.* TO 'consorcio_user'@'localhost';
# FLUSH PRIVILEGES;